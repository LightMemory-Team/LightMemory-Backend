# 首頁（圖2-5-5）API 需求文件

範圍：只做首頁，不含註冊／登入畫面、新手教學。

---

## 一、待確認事項結論

| 項目 | 結論 |
|---|---|
| 通知鈴鐺數字徽章 | 要加 |
| 大腦訓練遊戲卡片 | 只留「菜市場」1張正式卡片，刪掉「尚未開發」佔位卡片 |
| 問候副標 | 依日期給予不同句子（後端算好、傳固定字串給前端，前端不用自己判斷邏輯） |
| 動態牆顯示則數 | 只顯示最新 1 則 |
| API 是否需要登入身份 | 需要（驗證方式：Token Authentication 或 JWT，待後端決定） |
| API 呼叫時機 | 只在使用者「一進首頁」呼叫一次，不需要下拉重新整理再呼叫 |
| 動態牆空狀態 | 前端自行處理：當 wall_posts 為空陣列時，前端顯示固定提示句（引導去玩聲影日記），不需要後端額外欄位 |

**備註（API 呼叫時機）**：因為只在「進首頁」呼叫一次，代表首頁畫面不需要做下拉重新整理（Pull-to-refresh）功能。Flutter 端建議用 IndexedStack 保留首頁狀態，切到別頁再切回來不會重新呼叫 API。

---

## 二、API 設計方向

建議做法：這頁所有資料合併成**一支** API（`GET /api/home`），一次回傳整頁需要的東西，而不是每個區塊各打一支 API——使用者打開首頁只需要等一次網路請求，速度較快、也較好維護。

---

## 三、API 需求表

| 畫面元素 | 建議欄位名稱 | 型別 | 範例值 | 備註 |
|---|---|---|---|---|
| 使用者姓名 | `user_name` | string | "玉蘭" | 前端自己組「早安，玉蘭！」，後端只給名字，不要給「王奶奶」這種稱謂 |
| 問候副標 | `daily_tip` | string | "今天也要保持大腦活力喔。" | 依日期算好回傳固定字串，同一天不管呼叫幾次都相同 |
| 每日建議文字 | `daily_suggestion.text` | string | "完成一場菜市場遊戲" | |
| 每日建議點擊導向 | `daily_suggestion.action_route` | string | "game_market_sort" | 點擊後要導去哪個遊戲，用代碼給，前端對應到畫面路徑 |
| 未讀通知數 | `unread_notification_count` | int | 3 | 顯示於鈴鐺數字徽章，0 則不顯示徽章 |
| 遊戲卡片列表 | `games[]` | array | 見下方 | 目前固定只有一項 |
| ── 卡片 ID | `games[].id` | string | "market_sort" | |
| ── 卡片名稱 | `games[].title` | string | "菜市場" | |
| ── 是否已開發 | `games[].is_developed` | boolean | true | false 時前端顯示「尚未開發」佔位樣式 |
| 動態牆貼文列表 | `wall_posts[]` | array | [] 或 1 筆資料 | 只回傳最新 1 則；空陣列時前端顯示引導提示，後端不用額外處理 |
| ── 作者姓名 | `wall_posts[].author_name` | string | "我" | |
| ── 頭像網址 | `wall_posts[].avatar_url` | string / null | null | 沒有頭像時前端顯示預設圖示 |
| ── 發文時間 | `wall_posts[].time_ago` / `posted_at` | string (timestamp) | | 建議傳實際時間戳記，前端自己轉「昨天」等相對時間 |
| ── 內容文字 | `wall_posts[].content_text` | string | "昨天練習了書法，感覺心神寧靜，真是有趣的一天！" | |
| ── 讚數 | `wall_posts[].like_count` | int | 24 | |
| ── 留言數 | `wall_posts[].comment_count` | int | 5 | |

---

## 四、待後端確認事項

1. **驗證方式**：Token Authentication 還是 JWT？（需補進本表最上方）
2. **是否需要登入身份**：需要，因為資料是「這位使用者」專屬的
3. **呼叫時機**：使用者一進首頁就呼叫一次即可
4. **空資料處理**：若使用者第一次使用、完全沒有動態牆貼文/建議資料 → 會出現提醒使用者去玩聲影日記的提示句（前端自行處理，不需要後端額外欄位）

---

## 五、與其他頁面 API 需求對照（待補齊細節）

以下頁面目前僅確認「是否需要 API」，尚未有完整欄位表：

| 編號 | 介面名稱 | 是否需要 API | 備註 |
|---|---|---|---|
| 圖2-5-3 | 註冊填寫資料頁面 | 是（送出註冊資料） | 需確認：姓名欄位從哪來？「帳號」是否等於電話/信箱？ |
| 圖2-5-4 | 進入遊戲頁面（歡迎過場） | 否（純畫面） | |
| 圖2-5-6 | 會員中心 | 是（會員資料、家屬綁定列表） | 需確認：綁定邏輯是長者主動加還是家屬主動綁 |
| 圖2-5-11~15 | 新手教學頁面 | 否（純畫面互動） | |
| 圖2-5-23 | 資訊加油站（首頁） | 是（活動列表資料、定位距離） | 需確認：定位權限被拒時的替代顯示邏輯 |
| 圖2-5-27 | 詳細儀表板 | 是（認知分數、腦年齡數據） | 需確認：六軸雷達圖 vs 長條圖呈現方式 |
| 圖2-5-29 | 遊戲（首頁） | 是（今日完成度、推薦類別） | |

---

## 六、給後端組的訊息草稿

首頁範圍與規格已確認，附上最終版 API 需求表。目前只差一項：API 驗證方式要用 Token Authentication 還是 JWT？麻煩決定後補進表格最上方。前端會先用假 token 開發不會卡進度。其他頁面（會員中心、儀表板、資訊加油站、聲影日記）麻煩比照本表格式，整理出需要的欄位名稱、型別、範例值。
