# 菜市場找路 Z-score 設計（注意力維度）

這份文件討論「菜市場找路」（market_route）遊戲的分數要怎麼轉換成儀表板六認知
領域雷達圖裡「注意力」這一軸的分數。對應規格書「四、計分邏輯」與 Issue #10
（實作 DDA 動態難度調整邏輯：Z-score 三輪移動平均、六認知領域分數計算）。

**現況**：目前只有這個遊戲，其他 5 個維度（執行功能、工作記憶、數學、語言、
視覺空間）的計分方式都還在設計中，所以這份文件先在 `market_route` 範圍內
驗證邏輯，之後打算抽成跨遊戲共用的統計模組。

## 為什麼不能直接用 total_score

`market_score.py` 現有的 `total_score`（單場總分）是：

```
單題得分 = 基礎分 × 難度係數 + 速度加成
```

難度係數（basic ×1.0 / intermediate ×1.3 / advanced ×1.6）把「這場玩到多難」
跟「這場玩得多好」混在一起：同樣的正確率、反應時間，一路升到 advanced 的
場次分數天生就比較高。這會讓「總分變高」無法區分是「真的進步」還是
「這場剛好比較快升階」，直接拿它算 Z-score 會失真。

`total_score` 仍然保留，作為**遊戲內給長者看的激勵分數**（成就感、關卡感），
但不作為儀表板判斷進步的依據。

## 設計：拆階段指標 → 各自 Z-score → 加權合成一個維度分數

### 第一步：每個階段（basic / intermediate / advanced）各算三個原始指標

用這場遊戲 `step_records` 裡的原始資料計算，都只看**每題的第 1 次嘗試**
（`attempt_number == 1`），這樣正確率跟反應時間的定義口徑一致，不會被
「答錯重試」拉低反應時間或墊高正確率：

| 指標 | 定義 |
|---|---|
| 正確率 | 該階段內，第 1 次嘗試就答對的題數 ÷ 該階段作答題數 |
| 平均反應時間 | 該階段內，第 1 次嘗試且非超時的 `response_time_ms` 平均 |
| 收斂曝光時間 | 該階段**最後一題**答完時的 `exposure_time_ms`（代表這人在此階段穩定下來的反應速度下限） |

如果玩家這場完全沒有進到某個階段（例如整場都卡在 basic），該階段的三個
指標記為「無資料」，不強行補 0。

### 第二步：每個子指標對「自己過去的歷史分布」算 Z-score

同一階段、同一子指標，拿這次的值跟這個人過去所有場次（同階段）的
平均值、標準差比較：

```
z = (這次的值 - 歷史平均) / 歷史標準差
```

反應時間、收斂曝光時間是「越低越好」，所以計算後要**反向**（乘 -1），
讓三個子指標的 Z-score 統一「越高分代表越好」：

```
z_accuracy         = (accuracy - mean_accuracy) / std_accuracy
z_response_time     = -1 * (response_time - mean_response_time) / std_response_time
z_exposure_time     = -1 * (exposure_time - mean_exposure_time) / std_exposure_time
```

### 第三步：階段內加權平均

```
stage_score = 0.4 * z_accuracy + 0.3 * z_response_time + 0.3 * z_exposure_time
```

權重（0.4 / 0.3 / 0.3）是初始建議值，之後長者實測後可調整——這行為
跟規格書其他地方（例如閃現時間增減值 −100ms/+150ms）標註「建議起始值，
測試後再調整」的原則一致。

### 第四步：跨階段合成單一維度分數

用玩家這場「實際到達的最高階段」決定各階段權重，越高階權重越高
（代表更能反映真實能力上限），例如：

| 這場最高到達階段 | basic 權重 | intermediate 權重 | advanced 權重 |
|---|---|---|---|
| 只到 basic | 1.0 | – | – |
| 到 intermediate | 0.3 | 0.7 | – |
| 到 advanced | 0.2 | 0.3 | 0.5 |

```
attention_score = Σ (該階段權重 × 該階段 stage_score)，只計入有資料的階段
```

這樣「一直卡在 basic 但正確率很高」不會被誤判成整體表現很好，也不會讓
「隨便晃進 advanced 一題」就佔掉過高權重（因為權重表是依「有沒有穩定
停留」設計，不是單純「有沒有踩過」）。

## 保底規則：樣本數不足時怎麼辦

Z-score 需要「歷史分布」，新使用者或玩不到幾場時，標準差可能是 0 或
無法計算，直接算會出現除以零或極端離群值，容易誤導。

**規則**：同一人、同一階段的歷史場次 **少於 3 場** 時：
- 該階段的 Z-score 不計算，回傳 `null`（前端顯示「資料不足，需要更多場次
  才能看出趨勢」，不是顯示 0 或假分數）
- 若整體維度分數所有階段都是 `null`，`attention_score` 也回傳 `null`

**過渡期做法（樣本數不足時的初始基準）**：在個人歷史場次不足 3 場的期間，
暫時借用「全體使用者在該階段的平均值/標準差」當作比較基準，等這個人自己
累積滿 3 場之後，才切換成用他自己的歷史分布重新計算。這讓新使用者一開始
也能有「大致落在哪個相對位置」的參考值，等資料夠了再讓比較對象「校準回
自己」。

## 尚待確認/校準的事項

- 階段內加權（0.4 / 0.3 / 0.3）與跨階段權重表，都是初始建議值，需要長者
  測試後调整，比照規格書其他地方的做法。
- 個人歷史資料要存在哪裡（目前 session 是暫時的 JSON 檔案，正式版本會
  隨 Issue #36/#37 的資料庫設計而定），這份文件只定義「怎麼算」，不涉及
  「存哪裡」。
- 「全體使用者」的過渡期基準，需要等有足夠多使用者資料後才有意義；
  在資料庫還沒上線、只有少數測試者的階段，這個過渡基準本身可能也不穩定，
  屆時需要重新評估。
- 這套「Z-score + 加權」邏輯設計時已經盡量不綁死菜市場找路的細節
  （只依賴「階段」「正確率」「反應時間」「收斂速度」這種抽象概念），
  之後其他 5 個維度若要沿用，需要各自定義自己的「子指標」對應到這套框架。
